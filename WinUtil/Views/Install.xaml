<UserControl x:Class="WinUtil.Views.Install"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Search Winget:" Foreground="White" VerticalAlignment="Center" Margin="0,0,10,0" Opacity="0.5"/>
                <TextBox x:Name="SearchBox" Width="300" Height="30" 
                         VerticalContentAlignment="Center" 
                         Background="#1A1A1A" Foreground="White" 
                         BorderBrush="#333333" Padding="8,0"
                         TextChanged="SearchBox_TextChanged"
                         KeyDown="SearchBox_KeyDown"/>
                <Button Content="Search" Width="80" Height="30" Margin="10,0" 
                        Style="{StaticResource RoundedButtonStyle}" Click="Search_Click"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Upgrade All" Width="100" Height="30" Margin="4,0" Style="{StaticResource RoundedButtonStyle}" Click="UpgradeAll_Click"/>
                <Button Content="Install Selection" Width="140" Height="30" Margin="10,0,0,0" Style="{StaticResource BlueRoundedButtonStyle}" Click="Install_Click"/>
            </StackPanel>
        </Grid>

        <TextBlock x:Name="StatusLabel" Grid.Row="1" Text="Featured Apps" Foreground="#0078D4" FontSize="14" FontWeight="SemiBold" Margin="5,0,0,10"/>

        <Border Grid.Row="2" Background="#1A1A1A" CornerRadius="8,8,0,0" BorderBrush="#252525" BorderThickness="1">
            <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="10">
                <ItemsControl ItemsSource="{Binding AllApps}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="4" VerticalAlignment="Top"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#222222" CornerRadius="5" Margin="6" Padding="12" BorderBrush="#333333" BorderThickness="1">
                                <StackPanel>
                                    <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" Foreground="White" FontSize="13" FontWeight="SemiBold">
                                        <TextBlock Text="{Binding Name}" TextTrimming="CharacterEllipsis" MaxWidth="180"/>
                                    </CheckBox>
                                    <TextBlock Text="{Binding Id}" Foreground="#666666" FontSize="11" Margin="26,4,0,0" TextTrimming="CharacterEllipsis" MaxWidth="180"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <Border Grid.Row="3" Background="#111111" CornerRadius="0,0,8,8" BorderBrush="#252525" BorderThickness="1,0,1,1" MinHeight="55" Padding="10">
            <StackPanel>
                <TextBlock Text="SELECTED APPS:" Foreground="#555555" FontSize="10" FontWeight="Bold" Margin="5,0,0,5"/>
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding SelectedApps}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#0078D4" CornerRadius="4" Margin="4,0" Padding="8,4">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Name}" Foreground="White" FontSize="11" VerticalAlignment="Center"/>
                                        <Button Content="✕" Foreground="White" Background="Transparent" BorderThickness="0" Margin="8,0,0,0" 
                                                FontSize="10" Cursor="Hand" Click="RemoveSelection_Click" Tag="{Binding}"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>